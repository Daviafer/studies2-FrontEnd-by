/* 
   SLICE  = COPIANDO ARRAY
   SPLACE = REMENDAR ou costurar (remover um item pela posição do índice e gerar um novo array)
*/
let frutas = ['Banana', 'Maçã', 'Laranja', 'Uva' , 'Pera']

// let frutasExtraidas = frutas.slice(1,3)  // (1,3) = ele pega a última posição e subtrai 
//let frutasExtraidas = frutas.slice(1) // todos após Maçã (1)


//let frutasExtraidas2 = frutas.splice(1,2) // retirando maçã e laranja


// let removeEAdicionaFrutas = frutas.splice(1,2, 'Abacaxi') // = retirando maçã e laranja e adicionando abacaxi

console.log(frutas)

